# boids
Boid simulation in r7rs scheme made and reliant on racket.

## Usage
to run the boid simulation launch main.rkt using the racket interpreter for scheme and make sure you installed the repo folder as a packet in racket either through Drracket gui or with raco if using commands.

## Parameters
in constants.rkt the changable parameters are listed, and can be changed there accordingly.




